import { followedBySemi, generator } from "./fancy-function-import";

const plugin = {
  insertText: async function(app) {
    followedBySemi({ "mymymy": "ohmy" }, [ "cat" ]);
    generator("jingle");
  }
}
